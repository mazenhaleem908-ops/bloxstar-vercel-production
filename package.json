{
  "name": "bloxstar-vercel-production",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "migrate": "tsx scripts/migrate.ts",
    "seed": "tsx scripts/seed.ts",
    "start": "vercel dev",
    "test": "vitest run"
  },
  "dependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "@vercel/node": "^5.0.0",
    "pg": "^8.13.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
